# Security Policy

## 报告安全问题

请优先使用仓库 **Security** 页面中的私密漏洞报告入口。如果该入口暂不可用，可以创建一个
不含利用细节的普通 Issue，请维护者建立私密沟通渠道；不要在公开 Issue、日志或截图里发布
用户配置、路径、凭据、prompt、transcript 或可直接复现攻击的细节。

## 重点范围

- 安装、升级和卸载脚本是否可能删除范围之外的文件
- Hook 配置是否覆盖或破坏其他应用已有的 hooks
- 本地 socket 的目录、权限、消息大小与输入过滤
- 鼠标接管后是否能通过 `Esc`、`Q`、右键、超时或进程退出安全恢复
- Release、SHA-256、下载地址或版本选择是否可能被替换或降级

## 当前发布信任边界

公开预览版使用 ad-hoc 签名，不包含 Apple Developer ID 公证或 Windows Authenticode 签名。
SHA-256 用来发现下载损坏或与发布页不一致，不能替代商业代码签名。用户应只从本仓库的
GitHub Releases 下载。

AgentAim 主应用不建立外部网络连接，不使用遥测。安装器下载 GitHub Release；可选 Hook 只在
本机传递经过白名单过滤的生命周期元数据，不发送 prompt、transcript、工具输入输出、cwd 或
错误正文。

## 支持范围

安全修复优先进入最新公开预览版。项目尚未承诺长期支持旧版；如安全问题影响旧版本，发布说明
会明确给出受影响范围和升级建议。
