[package]
name = "agentaim-core"
description = "AgentAim 的可移植玩法核心：跨平台共享的规则与协议，不含任何窗口/渲染/输入代码"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
